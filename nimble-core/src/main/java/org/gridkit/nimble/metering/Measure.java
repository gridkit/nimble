package org.gridkit.nimble.metering;

public enum Measure {
		
	NAME,
	MEASURE,
	/**
	 * Timestamp for single even or interval end for time intervals
	 */
	TIMESTAMP,
	START_TIMESTAMP,
	
}
